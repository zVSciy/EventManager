# Config file for Air
[build]
  cmd = "go build -o bin/main cmd/api/main.go"
  bin = "bin/main"
  dir = "."
  include_ext = ["go"]
  exclude_dir = ["vendor", "tmp"]
  exclude_regex = ["_test.go"]
  run_after_build = "chmod +x main"

[log]
  level = "debug"